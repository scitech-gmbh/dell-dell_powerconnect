# DELL PowerConnect Mock via bash
export PATH="$HOME/bin:$PATH"
export PS1="console>"
alias enable="read -p Password:; export PS1='console#'"
alias sh=show
alias reboot="read -p 'Are you sure you want to reload the stack' yn; exit"
